#include "main.h"

/**
 * add - this function adds two numbers
 *@a: the parameter to be used
 *@b: parameter to be used
 * Return: the value of two numbers added
 */

int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
