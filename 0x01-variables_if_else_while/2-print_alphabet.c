#include <stdio.h>

/**
 * main - the entry function to this program
 *
 * Return: return nothing in this case
 */

int main(void)
{
	int i;

	for (i = 'a' ; i <= 'z' ; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
