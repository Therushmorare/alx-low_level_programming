#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 *_strlen - checks for length of string
 *@*s: pointer to be used
 *
 *Return: always return zero
 */

int _strlen(char *s)
{
	return (strlen(s));
}
